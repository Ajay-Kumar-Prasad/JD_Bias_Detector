# Core NLP
transformers==4.40.0
torch==2.3.0
sentencepiece

# API
fastapi
uvicorn[standard]
pydantic
python-multipart
httpx

# LLM (rewriter)
anthropic
openai

# UI (only if same repo used for Streamlit deploy)
streamlit

# Utils
python-dotenv
huggingface-hub